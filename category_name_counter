SELECT COUNT(category_name) from transactions WHERE category_name <> 'NULL' AND  created_at>={{dateRange1.value.start}}::timestamp with time zone AND created_at<={{dateRange1.value.end}}::timestamp with time zone ;
